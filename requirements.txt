# Local dev dependency: the shared business logic (database, invoice
# generation, matching, etc.) lives in the Gear-Ledger repo, on the
# web-app/installable-core branch, checked out as a sibling worktree.
# This is an editable install so changes there are picked up without
# reinstalling. Before deploying anywhere, this needs to become a pinned
# git+https dependency instead (once that branch/commit is pushed).
-e ../Gear-Ledger-web-app

# gearledger's pyproject.toml declares no runtime dependencies of its own
# (it's meant to be installed alongside the desktop app's requirements.txt,
# which isn't relevant here) — so this repo pins the actual runtime deps
# gearledger.server needs. Add pandas/openpyxl when Phase 2's invoice/
# completeness routes (which pull in invoice_generator.py/excel_utils.py)
# get wired up — not needed for this skeleton.
flask>=3.0.0
flask-cors>=4.0.0

gunicorn>=21.2.0
